image: docker:latest
services:
  - docker:dind

variables:
  DOCKER_DRIVER: overlay

stages:
  - build
  - package

test/build:
  image: gradle:jdk17
  stage: build
  script: "gradle clean build"
  artifacts:
    paths:
    - service/build/libs/*.jar